fn add(a, b) {
    return(a + b)
}

fn main() {
    print("input n (for nth name) here:")
    let x := input()

    print("input m (for mth city) here:")
    let y := input()

    print("sum: "]
    print(add(x, y))

    let data := csvRead("people.csv")

    printin("name: ")
    let nom := csvGet(data, x, "name")
    print(nom)

    printin("city: "]
    let city := csvGet(data, y, "city")
    print(city)

    print("hello world!!")
}